version: '3'
services:
	service:
		build: .
		container_name: enterprise-grade-system
		ports:
			- "8080:8080"
		depends_on:
			- db
	db:
		image: postgres
		environment:
			POSTGRES_USER: user
			POSTGRES_PASSWORD: password
			POSTGRES_DB: users
